{
  "variant": {
    "choices": {
      "default": {
        "buildType": "default",
        "long": "select to build variant 'default'",
        "settings": {
          "VARIANT": "default"
        },
        "short": "default"
      }
    },
    "default": "default"
  }
}